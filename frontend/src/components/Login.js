import React from 'react';

const Login = () => {
  // Directly render a message or redirect logic
  return (
    <div style={{ padding: 40, textAlign: 'center' }}>
      <h2>Welcome to the Life Makers Dashboard</h2>
      <p>Login is disabled. You can access the dashboard directly.</p>
    </div>
  );
};

export default Login; 